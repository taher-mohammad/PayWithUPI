/**
 * @format
 */

import {AppRegistry} from 'react-native';
import PayWithUPI from './src/PayWithUPI'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => PayWithUPI);
